import React, { Component, useState, useEffect } from "react";
import useForm from "../../component/useForm/useForm";
const About = (props) => {
    // tslint:disable-next-line: no-console
    // const { inputs, handleChange, handleSubmit } = useForm((inp) => console.log(inp),);
    return (
        <div>
            <h1>Under Construction</h1>
            {/* <form onSubmit={handleSubmit} noValidate>
                <input type="text" name="firstName" value={inputs.values ? inputs.values.firstName : ""} onChange={handleChange} required pattern="test" />
                {inputs.errors ? inputs.errors.firstName : ""}
                <div className="clearfix"></div>
                <button type="submit">Submit</button>
            </form> */}
        </div>
    );
};

export default About;
